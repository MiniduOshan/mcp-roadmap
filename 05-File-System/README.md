# 05 - File System MCP

This project implements an MCP server capable of interacting with the local file system.
Tools will include operations like `readFile`, `writeFile`, `listFiles`, and `searchFile`, allowing the AI to effectively browse and manage your local projects.

**Concepts Covered:**
- File System API Integration
- Advanced Tool Execution
- State manipulation via tools
