# 11 - Docker

This folder covers deploying and containerizing your MCP servers using Docker.
You will write `Dockerfile`s and `docker-compose.yml` to bundle your servers so they can run reliably anywhere.

**Concepts Covered:**
- Dockerizing Node.js MCP Servers
- Container Networking
- stdio vs SSE communication inside containers
